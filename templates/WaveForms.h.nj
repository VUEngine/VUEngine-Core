{%- import 'templates/partials/macros.nj' as macros -%}
{% include('templates/partials/fileHeader.h.nj') %}

#ifndef WAVEFORMS_H_
#define WAVEFORMS_H_


{{ macros.sectionHeader('Declarations') }}

{% if project.combined.items.WaveForm %}
{%- for waveForm in project.combined.items.WaveForm | values | unique('name') | sort(attribute='name') -%}
extern const int8 {{ waveForm.name }}WaveForm[];
{% endfor -%}
{% endif %}

#endif