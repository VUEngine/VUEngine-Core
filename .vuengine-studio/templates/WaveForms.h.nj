{%- import 'partials/macros.nj' as macros -%}
{% include('partials/fileHeader.h.nj') %}

#ifndef WAVEFORMS_H_
#define WAVEFORMS_H_


{{ macros.sectionHeader('Declarations') }}

{% for waveForm in waveForms | unique('name') | sort(attribute='name') -%}
extern const int8 {{ waveForm.name }}WaveForm[];
{% endfor %}

#endif