///////////////////////////////////////////////////////////////////////////////////////////////////////////
//                              THIS FILE WAS AUTO-GENERATED - DO NOT EDIT                               //
///////////////////////////////////////////////////////////////////////////////////////////////////////////


//---------------------------------------------------------------------------------------------------------
//                                                INCLUDES
//---------------------------------------------------------------------------------------------------------

#include <Printing.h>


//---------------------------------------------------------------------------------------------------------
//                                              DECLARATIONS
//---------------------------------------------------------------------------------------------------------

extern BYTE {{ font.image | basename(false) }}Tiles[];


//---------------------------------------------------------------------------------------------------------
//                                               DEFINITIONS
//---------------------------------------------------------------------------------------------------------

CharSetROMSpec {{ font.name | toUpperSnakeCase }}_FONT_CH =
{
	// number of chars
	{{ font.characterCount * font.size.x * font.size.y }},

	// allocation type
	__NOT_ANIMATED,

	// char spec
	{{ font.image | basename(false) }}Tiles,
};

FontROMSpec {{ font.name | toUpperSnakeCase }}_FONT =
{
	// font charset spec pointer
	(CharSetSpec*)&{{ font.name | toUpperSnakeCase }}_FONT_CH,

	// character number at which the font starts, allows you to skip the control characters for example
	{{ font.offset }},

	// number of characters in this font
	{{ font.characterCount }},

	// number of characters per line in charset
	{{ font.charactersPerLine }},

	// size of a single character (in chars) ({width, height})
	{{ "{" }}{{ font.size.x }}, {{ font.size.y }}{{ "}" }},

	// font's name
	"{{ font.name }}",
};