{%- import 'partials/macros.nj' as macros -%}
{% include('partials/fileHeader.c.nj') %}

{{ macros.sectionHeader('Includes') }}

#include <VIPManager.h>


{{ macros.sectionHeader('Definitions') }}

BrightnessRepeatROMSpec {{ v.name }}BrightnessRepeat =
{
	// mirror spec?
	{% if v.mirror %}true{% else %}false{% endif %},

	// brightness repeat values
	{
		{% for value in v.values -%}
		{%- if not (v.mirror and loop.index > 48) -%}
		{%- if value < 10 -%}{{ ' ' }} {%- endif -%}
		{{- value -}}{{- ', '-}} 
		{%- if loop.index % 16 == 0 and not (loop.last or (v.mirror and loop.index == 48)) %}{{ "\n		" }}{% endif -%}
		{%- endif -%}
		{%- endfor %}
	}
};