///////////////////////////////////////////////////////////////////////////////////////////////////////////
//                              THIS FILE WAS AUTO-GENERATED - DO NOT EDIT                               //
///////////////////////////////////////////////////////////////////////////////////////////////////////////


//---------------------------------------------------------------------------------------------------------
//                                                INCLUDES
//---------------------------------------------------------------------------------------------------------

#include <VIPManager.h>


//---------------------------------------------------------------------------------------------------------
//                                               DEFINITIONS
//---------------------------------------------------------------------------------------------------------

{% if colTable.description -%}
// {{ colTable.description }}
{% endif -%}
ColumnTableROMSpec {{ colTable.name }}ColumnTable =
{
	// mirror spec?
	{% if colTable.mirror %}true{% else %}false{% endif %},

	// column table spec
	{
		{% for value in colTable.values -%}
		{%- if not (colTable.mirror and loop.index > 128) -%}
		{{- (value.repeat * 16 + value.time) | intToHex(2) -}}{{- ', '-}} 
		{%- if loop.index % 8 == 0 and not (loop.last or (colTable.mirror and loop.index == 128)) %}{{ "\n    	" }}{% endif -%}
		{%- endif -%}
		{%- endfor %}
	}
};